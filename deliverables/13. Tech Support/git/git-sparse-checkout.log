기본적으로 clone 하면 모든 폴더가 복제되지만 git 1.7부터 추가된 sparse checkout 이라는 기능을 사용하면 원하는 경로의 디렉터리나 파일만 복제할 수 있다.

mkdir test && cd test 

git init

git config core.sparseCheckout true

# Download 받을 특정 파일과 디렉토리를 .git/info/sparse-checkout에 지정한다.
echo "sample-java-workspaces/microSvc/microSvc1/src/main/resources/static/" >> .git/info/sparse-checkout

git remote add -f origin https://github.com/herasoo/ktds-msa-proj-share.git

git pull origin master

# 추후 .git/info/sparse-checkout 수정 시 추가 설정 정보에 맞추어 업데이트 가능하다.
git read-tree -m -u HEAD